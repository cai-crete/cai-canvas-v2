# 롤백 계획: 현재 미커밋 변경사항 → HEAD (d35787f) 복원

**기준점**: `260428-220000-progress.txt` 기록 시점 = 커밋 `d35787f`  
**롤백 대상**: 그 이후 추가된 **미커밋 변경사항 8개 파일**

---

## 롤백 이유

`260428-220000-progress.txt`에 기록된 완료 상태(3가지 UI 버그 수정)까지만 유지하고,  
그 이후 추가된 **스트로크 영구 저장 / 배경 합성 기능 작업**을 되돌린다.

---

## 롤백 대상 파일 (7개) — API routes + ExpandedSidebar 제외

| 파일 | 변경 내용 (롤백 후 사라지는 것) |
|------|-------------------------------|
| `components/SketchCanvas.tsx` | `exportPathsJSON`, `loadPathsJSON` 메서드, `pendingImageTransformRef` 제거 |
| `app/page.tsx` | `compositeImages()` 함수, `strokeData`/`planBaseImageData` 파라미터, `handleGeneratePlanComplete` async 합성 로직 제거 |
| `components/ExpandedView.tsx` | `strokeData?` / `planBaseImage?` 파라미터 타입 제거 |
| `sketch-to-image/ExpandedView.tsx` | `strokeData` 저장/복원 로직, `loadPathsJSON` 호출 제거 |
| `sketch-to-plan/ExpandedView.tsx` | `compositeSketchOnBackground()` 함수, `strokeData`/`planBaseImageData` 전달 로직 제거 |
| `types/canvas.ts` | `strokeData?`, `planBaseImageData?` 필드 제거 |
| `sketch-to-plan/_context/protocol-sketch-to-plan-v3.8.txt` | 추가된 21줄 제거 |

## 유지 대상 파일 (3개)

| 파일 | 유지하는 변경 내용 |
|------|------------------|
| `app/api/sketch-to-image/route.ts` | `maxDuration` 300, fallback 모델 `gemini-2.5-pro` |
| `app/api/sketch-to-plan/route.ts` | fallback 모델 `gemini-2.5-pro` |
| `components/ExpandedSidebar.tsx` | `IconCollapse` 압축화살표 아이콘 |

---

## 롤백 실행 명령

```bash
git restore \
  project_canvas/app/page.tsx \
  project_canvas/components/ExpandedView.tsx \
  project_canvas/components/SketchCanvas.tsx \
  "project_canvas/sketch-to-image/ExpandedView.tsx" \
  "project_canvas/sketch-to-plan/ExpandedView.tsx" \
  "project_canvas/sketch-to-plan/_context/protocol-sketch-to-plan-v3.8.txt" \
  project_canvas/types/canvas.ts
```

---

## 롤백 후 상태 (HEAD d35787f 기준 + API routes 현재 상태)

`260428-220000-progress.txt` 기록의 완료 항목:
- [x] NodeCard 썸네일 `contain → cover`
- [x] SketchCanvas `referenceImageUrl` 줌 연동
- [x] PLANNERS 버튼 항상 활성화

미완 (브라우저 검증 필요 — 롤백 후에도 동일):
- [ ] 썸네일 cover 크롭 확인
- [ ] ExpandedView 줌 시 reference image 함께 확대 확인
- [ ] PLANNERS 버튼 동작 확인

---

## 체크리스트

- [ ] 사용자 승인 획득
- [ ] `git restore` 8개 파일 실행
- [ ] `git diff HEAD` 로 route 파일 2개만 남는지 확인
- [ ] progress 파일 저장
