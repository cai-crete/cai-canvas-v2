## 방금 한 것
- 다중 선택 Sketch-to-Image 빈 아트보드 생성 문제 수정

## 완료된 것

### project_canvas/app/page.tsx — handleGenerateComplete 수정 (1140~1195)
- `origin.sketchInputImages` 존재 여부로 분기 추가
- 다중 선택 분기 (sketchInputImages 있음):
  - 새 노드 생성 대신 origin 노드를 in-place 업데이트
  - artboardType: 'image', hasThumbnail: true, thumbnailData/generatedImageData 주입
  - multiSourceAnalysisReport 포함
  - 기존 엣지 재사용 (edgesRef.current 그대로)
- 단일 선택 분기 (sketchInputImages 없음):
  - 기존 동작 완전 유지 (새 노드를 오른쪽에 생성)

## 다음에 해야 할 것
- 실제 동작 테스트:
  1. 캔버스에서 image 아트보드 2개 다중선택 후 IMAGE 탭 클릭
  2. ExpandedView에서 GENERATE 클릭
  3. 빈 아트보드 없이 바로 이미지가 해당 아트보드에 표시되는지 확인
  4. 단일 선택 일반 스케치 → GENERATE 기존 동작 유지 확인
