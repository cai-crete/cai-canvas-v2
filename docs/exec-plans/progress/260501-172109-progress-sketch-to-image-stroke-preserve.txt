## 완료한 작업 (260501-172109)

### 작업: Sketch-to-Image Generate 이후 스트로크 편집 가능 유지

### 변경된 파일

1. project_canvas/sketch-to-image/ExpandedView.tsx
   - handleGenerate()에서 `canvas.exportState()`로 sketchPaths 추출
   - `onCollapseWithSketch?.(sketchBase64, thumbnailBase64, collectPanelSettings(), sketchPaths)` — 4번째 인자 추가
   - 이전: sketchPaths 누락 → Generate 후 re-expand 시 벡터 복원 불가
   - 이후: sketchPaths 포함 → Generate 후 re-expand 시 스트로크 편집 가능

### 핵심 동작 변화

- Sketch-to-Plan과 동일한 방식으로 Generate 시 벡터 스트로크 보존
- 노드 재확장 시 node.sketchPaths 우선 복원 로직이 이미 있었으므로 별도 수정 불필요

### 다음에 해야 할 것

- 실제 테스트: Sketch-to-Image 노드에서 Generate → Collapse → Re-expand 후 스트로크 편집 확인
