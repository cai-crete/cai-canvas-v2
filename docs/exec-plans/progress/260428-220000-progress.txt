작업: 3가지 UI 버그 수정 v2

방금 완료한 작업:

1. [Issue 1] NodeCard.tsx — 썸네일 objectFit contain → cover 복구
   - image 아트보드 케이스 (line 271) contain → cover
   - hasThumbnail 케이스 (line 283) contain → cover
   - ExpandedView reference image는 이미 height: '100%', width: 'auto' (높이 맞추기) 유지 — 무변경

2. [Issue 2] SketchCanvas.tsx — referenceImageUrl 오버레이 zoom 연동
   - img에 transform: translate(${internalOffset.x}px, ${internalOffset.y}px) scale(${internalZoom/100}) 추가
   - transformOrigin: 'center center'
   - 이제 스케치 캔버스 줌/패닝 시 배경 reference image도 함께 이동

3. [Issue 3] PLANNERS 버튼 항상 활성화
   - RightSidebar.tsx isTabDisabled: planners는 항상 false 반환 추가
   - page.tsx handleNodeTabSelect: planners 분기 추가
     - 선택 노드가 planners 타입 → expand
     - 그 외 → createAndExpandNode('planners') (자식 노드 생성)

완료된 이슈:
- 모든 3개 이슈 수정 완료
- exec-plan 체크리스트 완료 → completed 폴더로 이동

다음에 할 일:
- 브라우저에서 검증 필요:
  1. 썸네일이 cover로 크롭되어 표시되는지 확인
  2. ExpandedView에서 줌 컨트롤 사용 시 reference image가 스케치와 함께 확대되는지 확인
  3. sketch 아트보드 선택 시 PLANNERS 버튼이 활성화(검은색)인지 확인
  4. image 아트보드 선택 시 PLANNERS 버튼이 활성화인지 확인
  5. PLANNERS 클릭 시 새 planners 자식 노드 생성 + expand 진입 확인
