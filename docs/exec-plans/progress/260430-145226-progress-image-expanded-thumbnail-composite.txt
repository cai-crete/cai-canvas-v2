## 방금 한 것
Image Expanded(Sketch-to-Image) 썸네일에서 원본 이미지가 사라지는 버그 수정 완료.

## 변경 파일
1. project_canvas/components/SketchCanvas.tsx
   - `exportThumbnail(transparent?: boolean)` 파라미터 추가
   - transparent=true 시 흰 배경 fillRect 생략 → 스트로크만 투명 PNG 반환

2. project_canvas/sketch-to-image/ExpandedView.tsx
   - `compositeRefWithSketch(sketchThumbB64)` 헬퍼 추가
     : refImage(원본)를 배경으로 투명 sketch thumbnail을 오버레이 합성
   - `handleSketchCollapse` → async 변환 + 합성 로직 적용
   - `handleGenerate` → 합성 로직 적용 + deps 배열 업데이트

## 완료 상태
- tsc --noEmit 타입 에러 없음
- 모든 체크리스트 완료

## 다음에 할 것
- 실제 브라우저에서 동작 검증 필요:
  1. Image 노드 expand → 스케치 → collapse → 썸네일에 원본+스트로크 표시 확인
  2. 재차 expand → 스케치 → collapse → 썸네일 유지 확인
  3. 일반 sketch 노드(artboardType=sketch)는 기존과 동일 동작 확인
