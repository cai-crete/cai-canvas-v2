## 작업 완료: Sketch-to-Plan 지적도 미표현 / 그리드 출력 오류 수정

### 완료된 작업

#### Fix 1: SketchCanvas.tsx — exportStrokesOnly에서 그리드 제거
- `exportStrokesOnly()` (L.996~1035)에서 `if (gridModule) drawMajorGrid(...)` 제거
- `useCallback` dependency 배열에서 `gridModule` 제거
- `exportComposite()`의 `drawMajorGrid()` 호출은 유지 (Phase 1 공간분석용)
- 효과: sketch_image에 그리드가 bake-in되지 않아 AI가 출력에 그리드를 재현하지 않음

#### Fix 2: sketchToPlan.ts — Phase 2 이미지 파트 및 프롬프트 복원
- Phase 2 `contents`의 이미지 파트: `[sketchPart]` → `[...imageParts]`
  - 지적도가 있을 때: cadastral + composite + sketch 3장 전달
  - 지적도가 없을 때: sketch 단독 전달
- Phase 2 generationPrompt 재구성:
  - `cadastralContext` 삽입 (Phase 1과 동일한 이미지 역할 정의)
  - 지적도 있을 때: "IMAGE_1을 배경으로, IMAGE_2 스케치 위치 기준으로 배치" 지시
  - 지적도 없을 때: "흰색 배경" 지시 (조건부 분기)
  - "지적도 재현 금지" 삭제
  - building_footprint는 spatial-spec이 단일 권위 기준 유지
  - 스케치 이미지의 메이저 그리드 언급 제거 (그리드가 없어졌으므로)

### 수정 파일
- `project_canvas/components/SketchCanvas.tsx`
- `render-server/src/routes/sketchToPlan.ts`

### 판단 근거 (기록)
- Phase 1 외벽 혼동 실패: "어느 경계를 외벽으로 선택할지" 판단 과정의 실패
- Phase 2의 구조적 차이: spatial-spec이 building_footprint 좌표를 명시 → AI가 선택할 필요 없음
- 따라서 Phase 2에 지적도 재투입 시 외벽 혼동 위험은 Phase 1보다 구조적으로 낮음

### 다음 작업
- 배포 후 실제 생성 결과 확인 필요:
  1. 지적도가 있을 때: 생성 이미지에 지적도 배경이 표현되는지
  2. 건물 외벽이 스케치 외곽을 따르는지 (지적도 경계가 아닌)
  3. 생성 이미지에 그리드 선이 나타나지 않는지
  4. 지적도 없을 때: 흰 배경 평면도가 정상 생성되는지
