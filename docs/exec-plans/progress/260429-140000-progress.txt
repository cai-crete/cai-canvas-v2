## 진행 상황 — 2026-04-29 14:00

### 이번 세션에서 한 작업

**2D Map 썸네일 줌 조정 + UI 디자인 롤백 (3건)**

1. **썸네일 줌 조정**
   - CadastralMapView에 `zoomPadding` prop 추가 (기본값 2.5, 기존 동작 유지)
   - NodeCard 썸네일에서 `zoomPadding={0.5}` 전달 → 대지 주변 2-3건물만 보이게 줌인
   - ExpandedView는 기존 2.5배 패딩 유지

2. **썸네일 고정 설정값**
   - NodeCard 썸네일: `tmsType="Vector"`, `showSurrounding={true}`, `showLotNumbers={false}`, `fillSelected={false}`
   - ExpandedView 설정과 독립적으로 항상 고정

3. **UI 디자인 롤백 — Sketch 패널 패턴으로 통일**
   - CadastralPanel.tsx 전면 리라이트:
     - Tailwind 클래스 → 인라인 style + CSS 커스텀 프로퍼티
     - 별도 헤더 바 제거 (ExpandedSidebar가 타이틀 처리)
     - 섹션 라벨: Bebas 폰트, letterSpacing 0.1em
     - 토글: 직사각형 버튼 (borderRadius 0.75rem)
     - 바닥 버튼: pill 형태 (9999px), Bebas 폰트, "EXPORT TO IMAGE NODE"
     - 푸터: © CRETE CO.,LTD. 2026
   - Map3DPanel.tsx 동일 패턴으로 전면 리라이트

### tsc 결과
에러 없음

### 수정 파일
- project_canvas/components/CadastralMapView.tsx (zoomPadding prop)
- project_canvas/components/NodeCard.tsx (썸네일 고정 설정 + 줌)
- project_canvas/components/ExpandedSidebar/CadastralPanel.tsx (UI 롤백)
- project_canvas/components/ExpandedSidebar/Map3DPanel.tsx (UI 통일)

### 다음에 해야 할 것
- **브라우저에서 실제 확인** (가장 중요):
  1. 2D Map 썸네일 줌 레벨 확인 (대지 주변 2-3건물만 보이는지)
  2. 썸네일 배경이 벡터지도인지, 지번 숨김/색칠 없음 확인
  3. ExpandedView 진입 시 사이드바 디자인 확인 (Sketch 패널과 시각적 통일)
  4. 3D Map ExpandedView 사이드바도 동일 디자인 확인
  5. zoomPadding 0.5 값이 적절한지 — 너무 줌인/줌아웃이면 값 조정 필요
