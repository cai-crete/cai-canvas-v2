
날짜: 2026-04-29
세션: Phase 1 — Render Express 서버 구축 완료

== 방금 한 일 ==

render-server/ 디렉토리 신규 생성 (전체 구조):
- render-server/package.json          ← @google/genai, @supabase/supabase-js, express, cors
- render-server/tsconfig.json
- render-server/.gitignore
- render-server/src/index.ts          ← Express 앱 진입점, CORS 설정, 라우터 등록
- render-server/src/middleware/verifySecret.ts  ← x-internal-secret 헤더 검증
- render-server/src/lib/prompt.ts     ← buildSystemPrompt, loadProtocolFile (경로: ../../../project_canvas/)
- render-server/src/lib/architectStyles.ts ← getStylePrompt (7개 건축가 스타일)
- render-server/src/lib/supabaseUpload.ts  ← service role 키로 Storage 백그라운드 저장
- render-server/src/routes/sketchToImage.ts     ← N03 Gemini 로직 이전
- render-server/src/routes/sketchToPlan.ts      ← N02 Gemini 로직 이전
- render-server/src/routes/imageToElevation.ts  ← N04 Gemini 로직 이전
- render-server/src/routes/changeViewpoint.ts   ← N05 Gemini 로직 이전

project_canvas/ 수정:
- app/api/sketch-to-image/route.ts   ← 얇은 프록시로 교체
- app/api/sketch-to-plan/route.ts    ← 얇은 프록시로 교체
- app/api/image-to-elevation/route.ts ← 얇은 프록시로 교체
- app/api/change-viewpoint/route.ts  ← 얇은 프록시로 교체
- components/RenderWakeup.tsx         ← 신규, 앱 로드 시 Render 웨이크업
- app/layout.tsx                      ← RenderWakeup 컴포넌트 추가

TypeScript 검사:
- render-server/: tsc --noEmit → 오류 없음 ✅
- project_canvas/: print-proxy 관련 오류 1개 → 기존 pre-existing 오류 (우리 변경과 무관) ✅

== 현재 상태 ==
Phase 1 코드 작성 완료.
아직 Render Web Service 미생성 (Phase 0에서 계정만 만든 상태).

== 다음에 할 일 ==
Phase 2 완료 절차:
1. render-server/ 를 git commit & push (main)
2. Render Dashboard → New Web Service → GitHub 연결
   - Root Directory: render-server
   - Build Command: npm install && npm run build
   - Start Command: npm start
   - Environment Variables 입력:
     GEMINI_API_KEY_IMAGE, GEMINI_API_KEY_PLAN
     SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
     INTERNAL_SECRET
3. Render 배포 완료 후 서비스 URL 확인
4. Vercel 환경변수 업데이트:
   RENDER_SERVER_URL = https://{your-app}.onrender.com
   NEXT_PUBLIC_RENDER_HEALTH_URL = https://{your-app}.onrender.com
5. GET https://{your-app}.onrender.com/health → { status: 'ok' } 확인
6. Vercel 재배포 후 전체 E2E 테스트

작업: Zoom 동기화 & 스케치 영속성 수정

방금 완료한 작업:

[정리] docs/exec-plans/progress/old/
  - 260426-* 파일 6개 → old/ 로 이동

[Bug A] SketchCanvas.tsx — reference image CSS transform wrapper 교체
  - 기존: explicit px 좌표 (left: ox - cw*zs/2, width: cw*zs) → canvas와 별도 계산
  - 변경: CSS transform wrapper (position:absolute, left:0, top:0, transformOrigin:'0 0',
           transform: translate(${ox}px, ${oy}px) scale(${zs}))
  - 내부 image div: left:-cw/2, top:-ch/2, width:cw, height:ch (world 좌표)
  - 결과: canvas ctx.translate/scale과 동일한 변환 → 동기화 보장

[Bug A] InfiniteGrid.tsx — Math.round 제거
  - minor, bpx, bpy에서 Math.round 제거 → sub-pixel 오차 해소
  - grid와 canvas 정밀도 일치

[Bug B] SketchCanvas.tsx
  - SketchState import from @/types/canvas (로컬 정의 제거)
  - textItemsRef 추가 (useEffect로 textItems 미러링)
  - SketchCanvasHandle에 exportState() / loadState() 추가
  - useImperativeHandle에 구현: exportState는 현재 paths/uploadedImageData/imageTransform/textItems 반환
    loadState는 state 복원 후 undo 스택 초기화

[Bug B] types/canvas.ts
  - SketchState 인터페이스 추가 (paths, uploadedImageData, imageTransform, textItems)
  - CanvasNode에 sketchPaths?: SketchState 필드 추가

[Bug B] sketch-to-image/ExpandedView.tsx
  - onCollapseWithSketch 시그니처에 sketchPaths?: SketchState 추가
  - expand useEffect: node.sketchPaths 있으면 loadState() 우선, 없으면 기존 loadImage() 폴백
  - handleSketchCollapse: exportState() 호출 후 onCollapseWithSketch에 전달

[Bug B] sketch-to-plan/ExpandedView.tsx
  - 동일한 패턴으로 처리 (onCollapseWithPlanSketch, handlePlanCollapse)

[Bug B] components/ExpandedView.tsx (wrapper)
  - onCollapseWithSketch, onCollapseWithPlanSketch 시그니처 업데이트

[Bug B] app/page.tsx
  - SketchState import 추가
  - handleCollapseWithSketch: sketchPaths 파라미터 받아 updates.sketchPaths에 저장
  - handleCollapseWithPlanSketch: 동일 처리

TypeScript: 오류 없음 확인

완료 상태:
  [x] 260426-* 정리
  [x] Bug A 수정 (CSS wrapper + InfiniteGrid precision)
  [x] Bug B 수정 (sketchPaths 영속성 전체)
  [x] TS 오류 없음
  [x] exec plan → completed/ 이동

다음에 할 일:
  - iPad/browser에서 테스트 후 커밋 요청 시 진행

