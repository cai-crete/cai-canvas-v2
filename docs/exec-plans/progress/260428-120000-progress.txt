작업: Canvas ↔ Print 노드 연결 — 경로 A + BFF 구현 진행 중

완료된 작업:
1. 계획서 업데이트: docs/exec-plans/active/260428-print-canvas-connection-fix.md
   - 경로 A + BFF 확정 방향 반영
   - Phase 0~9 체크리스트 재구성
   - 보안 결함 ❼ 추가 (개방형 catch-all 프록시)

2. Phase 1 — .env.local.example 업데이트 ✅
   - PRINT_API_URL, CANVAS_API_SECRET, GITHUB_TOKEN 항목 추가

3. Phase 2 — .npmrc 생성 ✅
   - project_canvas/.npmrc: @cai-crete GitHub Packages 레지스트리 설정

4. Phase 3 — next.config.ts 업데이트 ✅
   - transpilePackages: ['@cai-crete/print-components'] 추가
   - webpack alias 8개 추가 (패키지 내부 @/ 경로 리디렉션)
   - 기존 OneDrive 캐시 비활성화 옵션 유지

5. Phase 4 — BFF 전용 라우트 생성 ✅
   - 신규: project_canvas/app/api/print-proxy/api/print/route.ts
   - POST 전용, Content-Type 검증, 20MB 제한, CANVAS_API_SECRET 필수 주입
   - 기존: [...]path]/route.ts — 아직 삭제 안 함 (사용자 확인 대기)

아직 완료되지 않은 작업:
Phase 0: Print 팀으로부터 CANVAS_API_SECRET 수령, 패키지 배포 확인 (외부 의존)
Phase 4 잔여: app/api/print-proxy/[...path]/route.ts 삭제 (사용자 확인 필요)
Phase 5: app/layout.tsx — print-tokens.css import (패키지 설치 후)
Phase 6: print/ExpandedView.tsx 교체 → npm 패키지 re-export (패키지 설치 후)
Phase 7: npm install @cai-crete/print-components (GITHUB_TOKEN 필요)
Phase 8: 로컬 연동 테스트
Phase 9: Vercel 환경변수 설정

다음에 해야 할 일:
1. 사용자에게 [...]path]/route.ts 삭제 확인 요청
2. Phase 0 (외부 의존) 완료될 때까지 대기
3. Phase 0 완료 후 → .env.local에 실제 값 추가 → Phase 7(npm install) → Phase 5, 6 순서로 진행

현재 상태:
- 즉시 가능한 코드 변경은 모두 완료
- 패키지 설치 전까지 빌드 시 next.config.ts의 webpack alias가 존재하지 않는 경로 참조 경고 발생 가능
  (단, transpilePackages는 패키지가 없을 때는 무시되므로 빌드 실패 아님)
- 새로운 BFF route.ts는 패키지 설치 여부와 무관하게 즉시 동작 가능
  (단, PRINT_API_URL, CANVAS_API_SECRET 환경변수가 있어야 실제 요청 성공)
