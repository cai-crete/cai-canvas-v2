## 작업 진행 상황

### 방금 완료한 작업
LibraryModal Signed URL 전환 완료

#### 수정된 파일
- project_canvas/components/LibraryModal.tsx
  - getThumbnailUrl(sync getPublicUrl) 제거
  - ThumbnailCard: useEffect + createSignedUrl(3600) 비동기 로드로 교체
  - src={url ?? undefined} 타입 처리

### 현재 상태
- tsc --noEmit 통과 (에러 없음)
- generated-images 버킷은 Private 유지 가능
- 썸네일은 1시간 유효 Signed URL로 로드됨

### 다음에 해야 할 작업
(수동) Supabase 대시보드 → Storage → generated-images 버킷 → Private 확인
Phase 4 (Library → Canvas 드래그 앤 드랍) — 보류 중
