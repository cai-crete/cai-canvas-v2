## 방금 한 일

생성 노드의 artboardType을 'sketch' → 'image'로 변경하여
IMAGE/PLAN 생성 결과 노드에서 ELEVATION, CHANGE VIEWPOINT, PRINT가 가능하도록 수정.

## 변경 파일

1. project_canvas/app/page.tsx
   - handleGenerateComplete (IMAGE 생성 완료): 자식 노드 artboardType 'sketch' → 'image'
   - handleGeneratePlanComplete (PLAN 생성 완료): 자식 노드 artboardType 'sketch' → 'image'

2. project_canvas/components/ExpandedView.tsx
   - artboardType==='image' && (type==='image' || type==='plan') 케이스 추가
   - 해당 노드 더블클릭 expand 시 스케치 에디터 대신 생성 이미지 뷰어 표시

## 완료된 것

- image -> generate -> 결과 노드(artboardType: 'image') → ELEVATION, CHANGE VIEWPOINT, DIAGRAM, PRINT 탭 활성화
- plan -> generate -> 결과 노드(artboardType: 'image') → 동일
- 결과 노드 더블클릭 → 생성 이미지 풀스크린 뷰어 표시

## 다음에 할 것

- 없음 (이번 요청 완료)
- 단, 기존에 저장된 노드(artboardType: 'sketch'인 이미 생성된 이미지 노드)는
  로컬 스토리지를 초기화해야 새 동작 확인 가능
