## 진행 상황 — 2026-04-29 17:45

### 이번 세션에서 한 작업

**VWorld 502 Bad Gateway 근본 원인 해결**

1. URLSearchParams → encodeURIComponent 변경 시도 → 효과 없음
2. Node.js https 모듈 → 표준 fetch + 재시도 복원 → 효과 없음
3. **근본 원인 발견**: Vercel 배포 리전 차이
   - Planners(`cai-planners-v2`): `vercel.json`에 `"regions": ["icn1"]` (서울) → 정상 작동
   - Canvas(`cai-canvas-v2`): 리전 미설정 → 미국 기본 리전 → VWorld 502
4. `project_canvas/vercel.json`에 `"regions": ["icn1"]` 추가 → 문제 해결
5. `route.ts`에 `preferredRegion = ['icn1', 'hnd1']` 추가
6. `docs/KNOWN_ISSUES.md` 생성 — 발생 가능한 9가지 문제점 정리

### 현재 상태

- VWorld WFS 502 에러 해결됨 (Vercel 서울 리전 배포)
- VWorld 2D 지적도 정상 작동 확인
- Planners 썸네일 수정 (이전 세션에서 커밋됨)

### 남아있는 작업

1. 3D Map 노드 정상 생성 여부 확인 (WFS 정상화로 자동 해결 예상)
2. 클라이언트 WMTS 벡터 타일 표시 확인 (NEXT_PUBLIC_VWORLD_KEY)
3. Planners 썸네일 표시 확인 (plannerMessages prop 수정 반영 확인)
