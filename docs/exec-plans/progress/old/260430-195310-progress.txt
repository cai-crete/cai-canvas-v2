## 작업 진행 상황

### 방금 완료한 작업
260430-auth-library-supabase.md Phase 3 (데이터 연결) 완료

#### 수정된 파일 목록

**render-server (서버 저장 로직)**
- render-server/src/lib/supabaseUpload.ts
  - uploadToStorage(nodeId, base64, mimeType, userId?, imageType?) — userId+type 있으면 generated_images DB insert
  - getUserFromToken(token?) — JWT → user_id 추출 helper 추가

- render-server/src/routes/sketchToImage.ts → type: 'sketch-to-image'
- render-server/src/routes/sketchToPlan.ts → type: 'plan'
- render-server/src/routes/imageToElevation.ts → type: 'elevation' (첫 번째 뷰만 DB 기록)
- render-server/src/routes/changeViewpoint.ts → type: 'viewpoint'
  (각 라우트: req.headers.authorization에서 JWT 추출 → getUserFromToken → uploadToStorage에 전달)

**Vercel 프록시 (Authorization 헤더 포워딩)**
- project_canvas/app/api/sketch-to-image/route.ts
- project_canvas/app/api/sketch-to-plan/route.ts
- project_canvas/app/api/image-to-elevation/route.ts
- project_canvas/app/api/change-viewpoint/route.ts
  (각 route: req.headers.get('Authorization') → Render 서버로 포워딩)

**프론트엔드 hooks (JWT 포함 요청)**
- project_canvas/hooks/useBlueprintGeneration.ts — Authorization: Bearer {token}
- project_canvas/hooks/usePlanGeneration.ts — Authorization: Bearer {token}
- project_canvas/hooks/useElevationGeneration.ts — Authorization: Bearer {token}
- project_canvas/app/page.tsx — handleGenerateViewpoint fetch에 Authorization: Bearer {token}

**라이브러리 모달 데이터 연결**
- project_canvas/components/LibraryModal.tsx
  - generated_images 테이블에서 탭별 type 필터로 fetch
  - 160×120px 썸네일 그리드 (auto-fill)
  - 로딩 상태 / 빈 상태 / 썸네일 카드 (날짜 표시)
  - storage URL: supabase.storage.from('generated-images').getPublicUrl()

### 현재 상태
- Phase 1, 2, 3 완료, 타입 에러 없음
- 썸네일 표시는 Supabase Storage 버킷이 public이어야 함
  → Supabase 대시보드 Storage → generated-images 버킷 → Make Public 필요

### 다음에 해야 할 작업
Phase 4: Library → Canvas 드래그 앤 드랍
- LibraryModal.tsx — ThumbnailCard에 draggable + onDragStart (application/cai-library)
- page.tsx — 캔버스 영역 onDrop + onDragOver 핸들러
- createNodeFromLibrary 함수 — Supabase signedUrl → 노드 생성 + pushHistory

### 배포 전 필수 확인
- Render 서버: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY 환경변수 등록 확인
- Supabase Storage: generated-images 버킷 public 설정 확인
- (또는 signed URL 방식으로 전환 — 버킷을 private으로 유지할 경우)
