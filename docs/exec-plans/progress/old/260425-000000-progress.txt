## 진행 상황 — 2026-04-25

### 방금 완료한 작업
N06 — change-viewpoint 프로토콜 이식 및 구조 정렬

### 변경 내용

1. **신규 생성: `project_canvas/change-viewpoint/_context/protocol-change-viewpoint-v1.txt`**
   - 기존 `route.ts` 인라인 상수 `PROTOCOL_VIEWPOINT_V1` 제거 → 별도 파일로 분리
   - 업로드된 `sys-prompt-changeviewpoint-v1.txt` 기준으로 재작성 (IVSP-Unified v1)

2. **신규 생성: `project_canvas/change-viewpoint/_context/knowledge-viewpoint-templates.txt`**
   - 업로드된 `template-analysis.txt`, `template-output1.txt`, `template-output2.txt` 통합
   - VIEWPOINT_SPEC (시점별 카메라/렌즈 스펙), 분석 출력 템플릿, 실행 프롬프트 예시 포함

3. **수정: `project_canvas/lib/prompt.ts`**
   - `loadProtocolFile()` 탐색 후보에 `change-viewpoint/_context/` 경로 추가

4. **수정: `project_canvas/app/api/change-viewpoint/route.ts`**
   - 인라인 상수(`PROTOCOL_VIEWPOINT_V1`, `VIEWPOINT_SPEC`, `buildAnalysisPrompt`, `buildGenerationPrompt`) 전부 제거
   - `loadProtocolFile` + `buildSystemPrompt` 패턴으로 교체 (STI/STP와 동일)
   - `callWithFallback` 시그니처를 STI/STP 클로저 패턴으로 통일
   - 모델: `gemini-2.5-pro-preview-05-06` / `gemini-2.0-flash-preview-image-generation` (최신 preview)
   - `generated_image`는 `data:image/*;base64,...` data URL 형식 유지
     (NodeCard.tsx에서 artboardType='image' 노드는 src를 직접 사용하므로)

### 빌드 결과
✓ npm run build 성공 (TypeScript 0 errors)

### 다음에 할 일
- Vercel 배포 후 change-viewpoint 실제 동작 검증
  - 4가지 시점(aerial, street, quarter, detail) 모두 테스트
  - 분석 텍스트(analysis) 사이드바 표시 확인
