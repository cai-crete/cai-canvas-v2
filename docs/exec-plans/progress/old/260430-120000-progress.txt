## 방금 한 것
- 작업지시서 기반으로 다중 아트보드 → Sketch-to-Image 구현 완료 (Tier 1~7)

## 완료된 것

### Tier 1 — types/canvas.ts
- CanvasNode에 `sketchInputImages?: (SelectedImage | null)[]` 필드 추가
  (인덱스0=평면도, 인덱스1=입면도)

### Tier 2 — app/page.tsx
- `SelectedImage` import 추가 (`@cai-crete/print-components`)
- `handleNodeTabSelect('image')` 에 다중선택(≥2) 분기 추가:
  - `isSketchArtboard()` 판별 → elevNode/planNode 자동 분류
  - `getNodeBase64()` 통일 추출 (generatedImageData ?? sketchData ?? thumbnailData)
  - 새 image CanvasNode에 sketchInputImages 주입
  - 다중 엣지 생성 (PRINT 패턴 동일)

### Tier 3 — sketch-to-image/ExpandedView.tsx
- `SelectedImage` import 추가
- `inputImages` state 추가 (node.sketchInputImages 초기값)
- `handleGenerate`에서 validInputSources → generate() 4번째 파라미터로 전달
- SketchToImagePanel에 inputImages, onInputImagesChange props 전달

### Tier 4 — components/panels/SketchToImagePanel.tsx
- `SelectedImage` import 추가
- props 추가: inputImages, onInputImagesChange
- IconSwap, IconClose SVG 아이콘 컴포넌트 추가
- handleRemoveSlot, handleSwap 핸들러 추가
- INPUT IMAGES 섹션 추가 (PROMPT 위, 다중선택 시에만 표시):
  - 평면도/입면도 2슬롯 고정 레이블
  - thumbnail 이미지 표시 (60×60 aspect-ratio 1:1)
  - × 제거 버튼 (슬롯 우상단)
  - 빈 슬롯: 점선 border + "비어있음"
  - ⇄ swap 버튼 (양쪽 모두 채워진 경우만 활성)
  - 섹션 하단 구분선

### Tier 5 — hooks/useBlueprintGeneration.ts
- `SelectedImage` import 추가
- generate() 시그니처에 `inputSources?: SelectedImage[]` 추가
- body에 input_sources 배열 추가 (roles: ['평면도','입면도'], idx 기반)

### Tier 6 — app/api/sketch-to-image/route.ts
- body를 그대로 전달하므로 변경 불필요 (JSON.stringify(body) pass-through)

### Tier 7 — render-server/src/routes/sketchToImage.ts
- InputSource 인터페이스 추가
- input_sources 파라미터 파싱
- isMultiSource 플래그로 단일/다중 분기
- imageParts 배열 구성 (단일 fallback 유지)
- Phase 1 프롬프트: 다중 소스 시 소스별 역할 컨텍스트 추가
- Phase 1/2 contents.parts에 imageParts 스프레드 적용

## 다음에 해야 할 것
- 실제 동작 테스트:
  1. 캔버스에서 image 아트보드 2개 다중선택 후 IMAGE 탭 클릭
  2. ExpandedView 진입 시 INPUT IMAGES 섹션에 평면도/입면도 슬롯 표시 확인
  3. ⇄ swap 버튼 동작 확인
  4. × 제거 버튼 동작 확인
  5. GENERATE 클릭 후 API input_sources 전달 확인
- render-server 빌드 및 배포 테스트
- 작업지시서 completed로 이동
