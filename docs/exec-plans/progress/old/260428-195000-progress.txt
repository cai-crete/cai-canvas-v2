작업: 검토 계획서(260428-canvas-print-integration-review.md) 즉시 실행 항목 적용

완료된 작업:

1. B-4 — next.config.ts bodySizeLimit 주석 정정
   - 기존: "BFF proxy route handler도 적용 (Next.js 15.1+)" (사실 오류)
   - 수정: action-handler 인터셉트 시 크기 제한이며, 없으면 기본 1MB → 413 발생 후 BFF 미실행됨을 명시

2. B-3 — library/route.ts maxDuration 추가
   - `export const maxDuration = 30;` 2번째 줄에 추가
   - Vercel 기본값(10s/15s) 의존 제거

3. B-2 — print/route.ts fetch 타임아웃 추가
   - `signal: AbortSignal.timeout(120_000)` 추가
   - 타임아웃 값은 응답 시간 측정 후 단축 검토 필요 (현재 임시값 120s)

4. B-5 — print/route.ts catch 블록 TimeoutError 분기 추가
   - `err.name === 'TimeoutError'` 분기: 504(Gateway Timeout) 반환
   - 기존 네트워크 실패: 502(Bad Gateway) 유지
   - AbortSignal.timeout()의 에러명이 'TimeoutError'임을 확인

5. tsc --noEmit 오류 0건 확인

현재 상태:
- B-2, B-3, B-4, B-5 모두 적용 완료
- TypeScript 오류 없음

보류 항목 (Print 팀 협의 필요):
- A-1: package.json file: 경로 → GitHub Packages (@cai-crete/print-components@0.1.1 배포 후)
- A-2: types/canvas.ts SelectedImage 타입 직접 import (A-1 완료 후)
- B-1: @/app alias 이름 변경 (Print 팀에 소스 vs 번들 배포 방식 확인 후)
- C-1: reactStrictMode: true 검증 (별도 브랜치, 안정화 후)
- C-2: content-length 헤더 제거 (선택, P1 묶음으로 처리)

다음에 해야 할 일:
- Print 팀으로부터 @cai-crete/print-components@0.1.1 GitHub Packages 배포 완료 통보 수령 후 A-1 수행
- A-1 완료 후 A-2 수행
- Print 팀에 패키지 빌드 방식(소스 vs 번들) 문의 후 B-1 결정
