## 진행 상황 — sketch 타입 노드 INPUT IMAGES 업로드 버그 수정

**세션 시각:** 2026-04-30 13:43:27

---

## 방금 한 일

다중 선택 시 `type === 'sketch'` 노드를 선택 후 [IMAGE] 클릭해도 INPUT IMAGES에 업로드되지 않는 버그 2개 수정.

---

## 수정 파일 및 내용

### `project_canvas/app/page.tsx`

**수정 1 — line 875 (필터 조건 추가)**
- Before: `n.artboardType === 'image' || n.artboardType === 'sketch'`
- After: `n.artboardType === 'image' || n.artboardType === 'sketch' || n.type === 'sketch'`
- 이유: sketch-type 노드는 `artboardType: 'blank'`로 생성되어 기존 필터를 통과하지 못했음

**수정 2 — line 885 (slot1 중복 방지)**
- Before: `const slot1 = elevNode ?? sketchInputNodes[1];`
- After: `const slot1 = (elevNode && elevNode.id !== slot0.id) ? elevNode : sketchInputNodes.find(n => n.id !== slot0.id) ?? sketchInputNodes[1];`
- 이유: 두 노드 모두 isSketchArtboard일 때 elevNode가 slot0과 동일 노드를 가리켜 INPUT IMAGES 양쪽 슬롯이 같은 이미지로 채워지는 버그 방지

---

## 완료 여부

- [x] 2개 수정 모두 완료
- [x] TypeScript 오류 없음 (tsc --noEmit 통과)
- [x] exec-plans/active 작업지시서 체크리스트 완료 표시

---

## 다음 해야 할 일

- 완료된 exec-plan 파일을 completed/ 로 이동 (체크리스트 모두 완료됨)
- 브라우저에서 실제 동작 확인 권장
