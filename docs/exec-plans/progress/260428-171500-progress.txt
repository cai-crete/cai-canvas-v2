작업: Phase 10 버그 수정 구현 완료

완료된 작업:
1. project_canvas/app/page.tsx (line ~569) — handleGeneratePrintComplete에서 generatedImageData 업데이트 제거
   - 변경 전: { hasThumbnail: true, thumbnailData: thumbnailBase64, generatedImageData: thumbnailBase64 }
   - 변경 후: { hasThumbnail: true, thumbnailData: thumbnailBase64 }
   - 효과: generatedImageData는 원본 canvas 소스 이미지 참조를 유지 (print 썸네일로 덮어쓰지 않음)

2. project_canvas/print/ExpandedView.tsx (line ~45) — sourceImage thumbnailData fallback 제거
   - 변경 전: const sourceImage = node.generatedImageData ?? node.thumbnailData;
   - 변경 후: const sourceImage = node.generatedImageData;
   - 효과: print 썸네일이 thumbnailData에 저장된 이후 재진입 시 이미지 슬롯에 잘못 주입되지 않음

3. project_canvas/components/NodeCard.tsx (line ~278) — hasThumbnail 브랜치 data URL 처리 수정
   - 변경 전: src={`data:image/png;base64,${node.thumbnailData}`}
   - 변경 후: src={node.thumbnailData.startsWith('data:') ? node.thumbnailData : `data:image/png;base64,${node.thumbnailData}`}
   - 효과: print 썸네일(완전한 data URL)이 이중 접두사 없이 정상 표시됨

수정한 파일은 모두 canvas 프로젝트 기존 파일 (새 파일 추가 없음):
- project_canvas/app/page.tsx
- project_canvas/print/ExpandedView.tsx
- project_canvas/components/NodeCard.tsx

아직 완료되지 않은 작업:
- Phase 10 테스트: 사용자가 브라우저에서 직접 확인 필요
  a. GENERATE → SAVE → Canvas 복귀 → 썸네일 정상(깨지지 않음) 표시 확인
  b. Print 노드 재진입 → 이미지 슬롯에 print 썸네일 미표시 확인 (빈 슬롯 또는 원래 삽입 이미지)
- Phase 9: Vercel 환경변수 설정 (배포 시)

다음에 해야 할 일:
1. 사용자 브라우저 테스트 결과 수령
2. 정상이면 Phase 10 완료 처리 및 계획서 active → completed 이동 검토
