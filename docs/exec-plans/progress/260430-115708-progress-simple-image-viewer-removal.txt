## 방금 한 것
- `project_canvas/components/ExpandedView.tsx`에서 단순 이미지 뷰어 분기 완전 제거
- `artboardType === 'image'` + `expandedViewMode === 'image' | 'default'` 이면 조건 없이 `SketchToImageExpandedView`로 라우팅
- `hasSketchContent` 조건 및 단순 이미지 뷰어 블록(img 태그 + LeftToolbar + ExpandedSidebar) 삭제
- tsc --noEmit 오류 없음

## 완료된 것
- ExpandedView.tsx 라우팅 단순화 완료
- 다중 선택 결과 노드 → SketchToImageExpandedView 진입 (분석 리포트 + 참조 이미지 overlay)
- 단일 스케치 결과 노드 → 동일하게 SketchToImageExpandedView 진입

## 다음에 해야 할 것
- 실제 동작 테스트:
  1. 다중 선택(평면도+입면도) → IMAGE → GENERATE → 결과 카드 더블클릭 → SketchToImageExpandedView 진입 확인
  2. 분석 리포트(ANALYSIS REPORT 섹션) 표시 확인
  3. 생성 이미지가 참조 overlay로 표시되는지 확인
  4. 단일 스케치 노드 Expand 동작 이상 없는지 회귀 테스트
